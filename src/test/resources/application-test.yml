  spring:
    datasource:
      url: ${TEST_DB_URL}
      username: ${TEST_DB_USERNAME}
      password: ${TEST_DB_PASSWORD}

  jpa:
    hibernate:
      ddl-auto: validate

  flyway:
    enabled: false